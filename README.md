# slugkit

Tiny, dependency-free helper that turns text into clean URL slugs.

```js
const slugify = require('slugkit');
slugify('Hello, World!'); // "hello-world"
```

## Install
```
npm install slugkit
```
